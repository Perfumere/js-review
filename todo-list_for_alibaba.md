# TodoList for Alibaba

## 20200731
- React:
```
    Function Component && Class Component ( hooks,高阶组件,工程化区别,hooks带来了哪些新的东西和问题,什么时候适合使用hooks) )
    异步数据更新(willRecieveProps & static getDerivedStateFromProps)
    this.setState 做了什么
    diff算法，组件刷新机制
    设计模式: MVVM , MVC
    redux
```
- JS:
> https://www.cnblogs.com/imwtr/p/9451129.html

```
    let const 箭头函数 this 结构赋值... Set、Map、WeakSet、WeakMap、Class
    闭包
    安全: CSRF/XSRF 跨站点请求伪造、 XSS 跨站点脚本攻击
    设计模式：工厂、单例、订阅发布、观察者、
    深拷贝
    Promise
    前端路由 hash、history模式
    模块化: AMD和require.js  CMD和sea.js CommonJS ESModule
```
- CSS:
```
    五大常用布局: float、单列、两列(自适应)、圣杯布局
    flex布局复习
    grid布局了解
    em/rem
    响应式布局 @media
```

- 网络:
```
    输入url回车发生了啥？hosts、代理？cname？
    http 1.1/2.0 相关请求头，状态码、复习强缓存、协商缓存
    tcp/udp 三次握手、四次挥手
    https 证书、对称加密？
    token、oAuth、session认证
    跨域: jsonp、cors、iframe
    websocket协议，心跳包
```

- H5:
```
    BFC 怎么生成(清除浮动、margin重合)
    localStorage、SessionStorage、indexedDB
    XMLHttpRequest 同步异步 get/post
    什么时候有options、复杂类型content-type?
    判断移动端和PC端
    自定义事件接口 addEventListener 对应的触发器
    MoutationObserver、requestAnimationFragment
    如何判断是否是一张图片
```

- 工具:
```
    postman
    rest-client
    webpack 及其并行打包优化配置
```
